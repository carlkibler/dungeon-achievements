# Your Cloudflare account ID (visible at dash.cloudflare.com → right sidebar)
CF_ACCOUNT_ID=

# Project-scoped Cloudflare API token — Analytics Read only for this account.
# Generate/rotate: source ~/.secrets && node scripts/provision-token.js
DA_CF_TOKEN=
